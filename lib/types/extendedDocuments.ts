// Define an interface that extends the standard Document interface
export interface ExtendedDocument extends Document {
	// Define a property "startViewTransition" of any type
	startViewTransition?: any;
}
